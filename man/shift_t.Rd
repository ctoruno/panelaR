% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnosis_utils.R
\name{shift_t}
\alias{shift_t}
\title{Shift a survey wave backwards in time}
\usage{
shift_t(year, period, back, by = c("month", "quarter"))
}
\arguments{
\item{year}{Integer vector. Wave year(s).}

\item{period}{Integer vector. Sub-annual period(s): month \code{1-12} when
\code{by = "month"}, quarter \code{1-4} when \code{by = "quarter"}.}

\item{back}{Integer. Number of periods to shift backwards.}

\item{by}{Character. Period granularity, one of \code{"month"} (default) or
\code{"quarter"}.}
}
\value{
A \code{\link[tibble]{tibble}} with integer columns \code{year} and
\code{period}, one row per element of the (recycled) inputs.
}
\description{
Moves a \code{(year, period)} wave back by \code{back} sub-annual steps,
rolling correctly across the year boundary (e.g. Q1 minus one quarter is Q4
of the previous year). Unifies the month-based look-back used by ENEMDU/ENE
with the quarter-based one used by ENOE.
}
\examples{
shift_t(2024, 2, back = 3, by = "month")    # 2023-11
shift_t(2024, 1, back = 1, by = "quarter")  # 2023 Q4

}
