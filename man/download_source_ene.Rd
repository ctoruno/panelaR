% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_source_ene.R
\name{download_source_ene}
\alias{download_source_ene}
\title{Download ENE CSV files from the INE source}
\usage{
download_source_ene(
  output_dir,
  start_year = 2020,
  end_year = as.integer(format(Sys.Date(), "\%Y")),
  overwrite = FALSE
)
}
\arguments{
\item{output_dir}{Character. Path to the directory where downloaded CSV
files will be saved. Created recursively if it does not exist.}

\item{start_year}{Integer. First year to download. Defaults to \code{2020}.}

\item{end_year}{Integer. Last year to download. Defaults to the current
calendar year.}

\item{overwrite}{Logical. If \code{FALSE} (default), files that already
exist in \code{output_dir} are skipped. Set to \code{TRUE} to
re-download and overwrite them.}
}
\value{
A character vector of file paths for all files that were downloaded
or already present on disk, returned invisibly.
}
\description{
Iterates over every rolling moving-quarter period between \code{start_year}
and \code{end_year} and downloads the corresponding CSV microdata file from
the Chilean National Statistics Institute (INE) public repository. Files
that are not yet published (e.g. future months of the current year) are
silently skipped after a HEAD check. Already-downloaded files are skipped
unless \code{overwrite = TRUE}.
}
